.contents__right-content--right-box__test-date{data: {id: "#{message.id}"}}
  .contents__right-content--right-box__test-date--test
    = message.user.name
  .contents__right-content--right-box__test-date--date 
    = message.created_at.strftime("%Y-%m-%d %H:%M")
.contents__right-content--right-box__hello{data: {id: "#{message.id}"}}
  - if message.content.present?
    %p
      = message.content
  = image_tag message.image.url if message.image.present?  

      